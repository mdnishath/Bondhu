package com.bondhu.app.ui.theme

import androidx.compose.ui.graphics.Color

object Tokens {
    val Primary    = Color(0xFFA3E635)   // lime-green accent
    val PrimaryDk  = Color(0xFF84CC16)
    val OnPrimary  = Color(0xFF0B1207)   // near-black text on lime
    val AppBg      = Color(0xFF0A0C0B)   // near-black background
    val Surface    = Color(0xFF14181A)   // cards
    val Header     = Color(0xFF161B1D)   // top bars / elevated (slightly glassy)
    val InBubble   = Color(0xFF181D1F)   // received message bubble
    val OutBubble  = Color(0xFF2A3A1E)   // own message bubble (dark olive-green, lime-tinted)
    val TextMain   = Color(0xFFF1F4EF)   // near-white
    val TextMut    = Color(0xFF93A08F)   // muted green-gray
    val TextFaint  = Color(0xFF5E6A5C)
    val Divider    = Color(0x14FFFFFF)   // ~8% white hairline
    val Tick       = Color(0xFFA3E635)   // lime ticks (read receipts)
    val Field      = Color(0xFF1C2123)   // input / chip background
    val Danger     = Color(0xFFFF6B6B)
    val Online     = Color(0xFF38EC48)   // bright green presence dot
}
